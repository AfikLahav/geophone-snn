# notebooks/ — Jupyter notebooks

Outputs are stripped (source only).

- `classical_separability.ipynb` — self-contained classical-ML separability + session-confound
  analysis on the real recordings. The reviewer-facing demo of the domain-gap problem.
- `geophone_snn_v2_train.ipynb` — the v2 SNN training notebook (script form: `scripts/training/geophone_snn_v2_train.py`).
- `geophone_snn_classifier.ipynb` — the original v1 prototype SNN (historical).
