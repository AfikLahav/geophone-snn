# scripts/plotting/ — figure generation

- `plot_132_findings.py` — confusion / AUROC figures for the 132-feature model.
- `plot_0977_stats.py` — stats/plot pack for a specific model result.
- `plot_real_recording.py` — waveform / PSD / ADC-quantization plot of a real recording.
- `plot_spikes.py` — spike-detection overview of a real recording.

Read model outputs (`../../models/`) + external data.
