# GeoSense backend dependencies (CPU-only — no GPU needed for inference).
# The first line points pip at PyTorch's CPU wheel index so a laptop without an
# NVIDIA GPU gets the small CPU build (not the ~2.5 GB CUDA build).
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.10.0
spikingjelly==0.0.0.0.14
numpy==1.26.4
scipy==1.15.3
PyWavelets==1.9.0
pandas==3.0.0
pyserial==3.5
fastapi==0.115.12
uvicorn[standard]==0.34.2
